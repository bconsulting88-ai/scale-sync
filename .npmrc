# Rebuild native modules (better-sqlite3, serialport) against Electron's Node
# electron-builder install-app-deps handles this automatically via postinstall.

# Use the Electron mirror for faster downloads (optional — comment out if on slow network)
# electron_mirror=https://npmmirror.com/mirrors/electron/

# Ensure npm uses the electron-specific node-gyp headers
# (electron-rebuild handles this — no manual config needed)
